// WiFi credentials per ESP32 Radar3D cross-ping firmware.
// Copia in secrets.h e personalizza. secrets.h e' in .gitignore.
//
// L'AP deve essere a 2.4 GHz e su canale 6 (vedi #define CHANNEL nello sketch).
// Per stabilita' fissa il canale dal pannello del router.

#pragma once

#define WIFI_SSID "ChangeMe"
#define WIFI_PASS "ChangeMe"
